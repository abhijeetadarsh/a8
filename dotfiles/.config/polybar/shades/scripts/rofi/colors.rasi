/*
 * Every rofi menu in this directory imports this file. It is a shim onto the
 * generated palette so the polybar menus, the launcher and the powermenu all
 * use the same colours as rofi itself.
 *
 * ../../color/out.rasi is written by theme_engine and is not in the repo.
 */
@import "../../color/out.rasi"
